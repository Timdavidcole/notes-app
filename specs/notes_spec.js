// feature test to create note
//go to to note.html
//click(add note)
//fill field with text "this is a new note. this is longer than 20 characters"
//click save
//expect page to have content "this is a new note. th"

//describe bookmark
  //it can create new bookmarks
  // #create
  // note1 = new note("this is the example body of text")
  // expect(note1.content).to eq ("this is the example body of text")
  // exoect(note1.title).to eq (note1.content.substr(0,20))
